{
  "expectedValid": false,
  "expectedErrors": [
    { "path": "/libraryRefs", "kind": "missing-required-property", "property": "icons" }
  ],
  "description": "libraryRefs missing required `icons` field — conformant validator must reject."
}
